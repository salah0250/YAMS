package com.example.joueur.service;
/*
import org.springframework.http.ResponseEntity;
import org.springframework.stereotype.Service;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestParam;
import com.example.joueur.client.StatisticsClient;
import reactor.core.publisher.Mono;

import java.util.List;
*/
public class JoueurService {
/*
    private final StatisticsClient statisticsClient;

    public JoueurService(StatisticsClient statisticsClient) {
        this.statisticsClient = statisticsClient;
    }

    public List<Double> getPlayerProbabilities(List<Integer> diceValues) {
        // Appeler le service de statistiques pour obtenir les probabilités
        Mono<List<Double>> probabilitiesMono = this.statisticsClient.getProbabilities(diceValues);

        // Convertir Mono en bloc pour obtenir la valeur
        List<Double> probabilities = probabilitiesMono.block();

        return probabilities;
    }

 */
}
